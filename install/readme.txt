======================
	自动安装的步骤
======================

1. 访问网站安装首页：
		1. 判断是否已经安装
			//判断是否已经安装过
			  if(file_exists("install.lock")){
				 die("网站已经安装过！" );
			  }
		2. 显示欢迎和协议信息，并提供下一步按钮
	
2. 执行环境监测：
		1. 判断某些目录是否具有指定的权限（可写）
			使用is_writable(OB_PATH.$v))判断是否有可写权限。
		
		2. 检测当前的安装环境。
				 如下是监测是否支持GD库
				 extension_loaded('gd') -- 基础是否装载gd模块
				 function_exists('imagecreate') -- 是否支持GD函数
				 
3. 提供配置信息的填写表单：
		    数据库主机：
			数据库名称：
			数据库用户名：
			数据库密码：
			数据表前缀：
			
			用户名：
			密码：
			确认密码：
			管理邮箱：
			演示数据：